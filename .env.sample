
# ローカルでの開発、テスト用の環境変数

API_URL=http://localhost:3000
API_SECRET=hogehoge
FIRESTORE_EMULATOR_HOST=test-firebase:8080

# Saxo OAuth token 暗号化用の 32 byte base64 key（実鍵は commit しない）
# 生成例: openssl rand -base64 32
SAXO_TOKEN_ENCRYPTION_KEY=
